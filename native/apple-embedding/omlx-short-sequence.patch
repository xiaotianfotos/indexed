diff --git a/omlx/patches/qwen35_ane_prefill.py b/omlx/patches/qwen35_ane_prefill.py
--- a/omlx/patches/qwen35_ane_prefill.py
+++ b/omlx/patches/qwen35_ane_prefill.py
@@ -320,9 +320,9 @@ def configure_qwen35_ane_prefill_scheduler(
-    if sequence_length < 1024 or sequence_length % 64:
+    if sequence_length < 64 or sequence_length % 64:
         raise ValueError(
-            "ANE prefill sequence_length must be a multiple of 64 >= 1024"
+            "ANE prefill sequence_length must be a multiple of 64 >= 64"
         )
@@ -3133,8 +3133,8 @@ def enable_qwen35_ane_prefill(
-    if sequence_length < 1024 or sequence_length % 64:
-        raise ValueError("ANE prefill sequence_length must be a multiple of 64 >= 1024")
+    if sequence_length < 64 or sequence_length % 64:
+        raise ValueError("ANE prefill sequence_length must be a multiple of 64 >= 64")
